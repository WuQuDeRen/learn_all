package com.learn.algorithm.sort.selection;

import com.learn.algorithm.sort.Sort;

/**
 * 选择排序的基本思想：每一趟从待排序列中选去一个关键字最小的记录，即第一趟从 n 个记录中选取关键字最小的记录，第二趟从剩下的 (n-1)个待排序记录中再选出关键字最小的记录，直到整个序列的记录选完为止.
 */
public interface SelectionSort extends Sort {

}
