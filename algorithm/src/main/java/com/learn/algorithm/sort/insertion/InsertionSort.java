package com.learn.algorithm.sort.insertion;

import com.learn.algorithm.sort.Sort;

/**
 * 插入排序的基本思想：通过构建有序序列，将待排序的数据，在已排好序的序列中从后向前扫描，找到其相应位置并进行移位操作，最后进行插入操作。通常需要一个辅助空间来存储当前待排序的记录。
 * 常见分类：直接插入排序、折半插入排序、shell排序
 */
public interface InsertionSort extends Sort {
}
